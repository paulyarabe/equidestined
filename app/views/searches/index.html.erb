<div class= "container">

  <div class="main-container index-attributes z-depth-5">
    <div class="static">
        <h3 class="txt-primary">Equidestined</h3>
      </div>
        <h6><%= @title %></h6>
        <ul>
        <% @searches.each do |search| %>
          <li><% user = User.find(search.user_id) %>
          <p><strong><span class="txt-primary"><%= user.name %></span> - <%= search.created_at.strftime("%A, %B %-d, %Y %I:%M:%S %P") %></strong></p>
            <ul>Locations searched:
              <% search.locations.each do |location| %>
              <li><%= location.address %> </li>
              <% end %>
            </ul>
            <strong>Midpoint:</strong>
            <%= search.midpoint.address %>
          </li>
      <% end %>
      </ul>
  </div>
</div>
